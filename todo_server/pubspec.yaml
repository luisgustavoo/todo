name: todo_server
description: Starting point for a Serverpod server.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  serverpod: 1.2.5
  serverpod_auth_server: 1.2.5
  googleapis_auth: ^1.5.0
  mailer: ^6.1.0
  logging: ^1.2.0

dev_dependencies:
  test: ^1.25.2
  hard_analyser: ^1.8.0
  mocktail: ^1.0.3
